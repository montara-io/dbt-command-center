export enum GenericStatus {
  pending = "pending",
  in_progress = "in_progress",
  completed = "completed",
  success = "success",
  failed = "failed",
  neutral = "neutral",
}

export enum AssetType {
  Model = "Model",
  Source = "Source",
}
